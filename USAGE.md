# LinguaFlix 使用教程

LinguaFlix 是一个"沉浸式"英语学习播放器：通过**挡住中文、只看英文**的方式逼自己听懂原声，配合截图、生词本和 AI 分析来积累词汇。

> **启动方式（推荐）**：双击桌面上的 **LinguaFlix** 应用 —— 这是真正的桌面版，独立窗口，不依赖 Chrome/浏览器。
> 开发者方式：运行 `./start.sh` 启动网页版（浏览器打开 http://localhost:5173，功能与桌面版一致）。

---

## 1. 界面总览

### 首页（Dashboard）
| 区域 | 作用 |
|------|------|
| 顶部统计 | 今日待复习单词数、连续学习天数、累计生词数 |
| 最近添加 | 最近保存的生词（点词可…） |
| 三张功能卡 | **Continue** 继续上次的视频 · **Review** 进入复习 · **New Video** 新建学习 |
| 我的视频 | 已学过的视频列表，点击打开，悬停出现删除按钮（点两下确认） |

### 播放页
```
┌────────────────────────────────────────────────┐
│ ← 返回     [ 视频地址栏 / 换视频 / 字幕 ]  [挡块⚙]│
│                                                │
│              视频画面                            │
│              （底部是字幕 + 字幕挡块）             │
│                                                │
│   [▶ ⏮ ⏭ 时间  音量 速度 截图]  ← 鼠标悬停出现   │
│                                                │
│   [ 字幕文本记录（Transcript，可折叠）]            │
├────────────────────────────────────────────────┤
│ 📓 笔记 / 生词         ← 右侧窄栏（悬停弹出）      │
└────────────────────────────────────────────────┘
```

---

## 2. 加载视频（三种方式）

点首页的 **New Video** 进入播放页，然后：

1. **打开本地视频文件**：点 `Open Video File`，选择电脑里的视频（mp4/mkv 等）。
2. **粘贴链接（YouTube / Bilibili）**：把视频页面网址粘进输入框。
   - 应用会自动用 yt-dlp **下载**到本地再播放，下方有下载进度条；
   - 可以直接粘贴 B 站 / YouTube 的**分享文案**（带【标题】的那种），会自动提取里面的链接；
   - 首次使用如果提示没有 yt-dlp，点 `Install yt-dlp` 一键安装。
3. **直接视频链接（.mp4 等）**：粘贴直链，立即播放，不下载。

视频加载后，顶部的地址栏会变成一个小胶囊：显示文件名 + `Change`（换视频）+ `Subtitles`（加载字幕）。

## 3. 加载字幕

- 在**欢迎页**（还没加载视频时）点下方的 `Load external subtitles (SRT/VTT)`；
- 视频加载后，点顶部胶囊里的 **Subtitles**。

支持 `.srt / .vtt / .ass / .ssa`。加载后：
- 视频底部会显示字幕（**中文** = 琥珀色黑底，**英文** = 白字黑底）；
- 视频下方的 **Transcript** 面板会显示完整字幕文本，**自动跟随当前播放行**，点任意一行可跳转；
- Transcript 面板右上角还有一个 **Set OCR region** 按钮（见下方「OCR 识别区域」）。

> 提示：很多 B 站视频自带双语 CC 字幕，可以先在 B 站打开"字幕"开关，再复制字幕文本存成 .srt 加载进来。也可以单独准备一个中英双语的 .srt。

---

## 4. 字幕挡块（核心功能）

挡块就是一个盖在字幕上的矩形，用来**把中文行遮住**，只留英文。

### 基本操作
| 操作 | 方法 |
|------|------|
| 移动 | 鼠标按住挡块中间，拖到想盖住的位置 |
| 缩放 | 拖拽挡块**边缘/四角**的手柄（悬停时出现） |
| 显示/隐藏 | 按 **B** |
| 锁定 | 按 **L**（锁定后不能移动/缩放，防止误碰） |
| 重置 | 按 **R**（恢复默认位置、大小、透明度、效果） |

### 右上角的 ⚙ 挡块设置面板
| 项 | 说明 |
|----|------|
| **Opacity（透明度）** | 滑条 10%–100%。默认 **100%**（完全不透明，彻底盖住中文） |
| **Effect（效果）** | 两个模式，点一下立刻生效： |
| | **Solid** — 实心深色块，把字幕完全盖住（默认） |
| | **Blur** — 模糊玻璃，把字幕**模糊成毛玻璃**但视频画面仍清晰可见 |
| **Position** | 一键摆放：Bottom（底部）/ Mid-low（中下）/ Middle（中间） |
| **Visible / Lock** | 显示切换 / 锁定切换（和 B / L 键一样） |
| **Reset** | 一键恢复所有默认值 |

> **挡块设置会自动保存**：位置、大小、透明度、效果都会记住，下次打开应用（或换视频）不用重新调。按 **R**（或 Reset）会恢复默认值，并同样保存下来。

> **什么时候用 Blur？** 觉得实心黑块太"挡画面"、想保留一点字幕残影感时，切到 Blur——中文字被糊掉，但你能透过毛玻璃看到画面细节，英文行照常清晰显示。

---

## 5. 截图 + 笔记（保存"场景"）

### 截图
- 按 **S**，或悬停底部控制条点相机图标。
- 会自动：**暂时隐藏挡块 → 截一帧 → 打上时间码 → 恢复挡块**，然后弹开右侧笔记栏。
- 屏幕下方会出现提示：**「截图已保存 · 快照 #N」**（N 是当前笔记里的第几张），一眼就知道生效了没、是第几张。
- 截图会**自动 OCR 识别**该画面**底部**的字幕区域（默认识别框覆盖到画面底边，正好罩住字幕）。
- **本地 PaddleOCR 引擎**：识别在浏览器本地完成（无需联网、不调用任何 API），用的是 PaddleOCR（PP-OCRv5）检测 + 识别模型。它会自动找出字幕框里每一行文字，**只取英文那一行**（中文行、水印、时间码等自动忽略），所以即使识别框横向拉满整个画面宽度，也不会再因为两侧空白/背景噪声被误读成乱码。
- **越用越准（纠错反馈）**：每次你手动改正一条识别错的字幕，应用都会记住这次改动——以后再识别到**一模一样的句子**会直接套用你的改法，识别到**同一个错词**（比如 `kness` → `knees`）也会自动替换。改得越多，识别越准。

### OCR 识别区域（每个视频设一次，识别更准）

每个视频的英文字幕位置基本是固定的，所以建议**第一次看这个视频时，手动框一次字幕区域**，之后所有截图的 OCR 都用它，识别率会高很多：

1. 播放视频，把进度条停到**有英文字幕的那一帧**（自选哪一帧都行）；
2. 点 Transcript 面板右上角的 **Set OCR region** 按钮（视频会自动暂停并抓取当前帧）；
3. 全屏弹窗里**拖动选框移动 / 拖四角调整大小**，把英文字幕那一行框进去；
4. 点 **Save region** 保存。

- 保存后按钮会变成绿色的 **OCR region set**，旁边多一个 **↺** 复位键；
- 该区域会**按视频自动记住**，下次打开同一个视频无需重新框选；
- 按 **↺** 可恢复成默认的底部区域。

> 提示：这个功能主要针对**内嵌（硬字幕）视频**——没有单独字幕文件、字幕是烧在画面里的那种。框好区域后，截图 OCR 就能稳定抓取字幕文字。

### 笔记侧边栏
- 屏幕最右侧有一根**窄竖条**（两个图标：📓 Notes、📖 Vocab）。
  - **鼠标悬停**在某个图标上 → **弹出**预览面板（不影响界面大小），预览的就是那个图标对应的页（悬停 Vocab 预览生词，悬停 Notes 预览笔记）；
  - **点击**图标 → 侧边栏**完整展开**（约占屏幕宽度的 **1/3**，操作更宽敞），并直接显示对应的页面。
- **Notes 页**：一张截图 = 一张卡片。展开卡片可以：
  - 点卡片左上角**缩略图** → **全屏放大**查看那一帧（方便确认画面是不是对得上字幕）；
  - 点 **时间码** → 跳回视频（从该时间点前 3 秒开始播放，6 秒后自动暂停，方便精听）；
  - 编辑/修正 OCR 出来的字幕文本；
  - `Re-recognize area`：以**全屏**方式显示截图，**拖动选框移动 / 拖角调整大小**重新框选字幕区域，再点 Recognize 重新 OCR（识别错了就用它）；
  - 点字幕里的**任意单词** → 弹出词典 + 保存 + AI 分析；
  - 写自己的笔记。
- **多个笔记**：点顶部笔记名可切换/新建/删除笔记（同一视频可分多个学习会话）。

---

## 6. 学单词（词典 + AI）

1. 展开一张截图卡片，在"Click a word"那行点一个单词；
2. 下方出现：
   - **词典**（Free Dictionary）：音标、词性、释义、例句；
   - **Save**：把词存进生词本（带当时的语境句子、时间点、截图）；
   - **AI**：让 AI 结合**当前语境**分析这个词的用法（需要先配置 API Key，见下）；
3. 存过的词会出现在卡片的 **Saved Words** 和侧边栏 **Vocab** 页里。

### 配置 AI API Key（目前无设置界面，用浏览器控制台）
AI 分析用的是你自己带的 OpenAI / Claude Key，存在浏览器本地：
```js
// 打开开发者工具（F12）→ Console，执行：
localStorage.setItem('linguaflix-settings-v2', JSON.stringify({
  aiProvider: 'openai',          // 或 'claude'
  aiModel: 'gpt-4o-mini',        // 或 'claude-3-5-sonnet-20241022'
  openaiApiKey: 'sk-你的key',     // 用哪个填哪个
  claudeApiKey: 'sk-ant-你的key',
}))
// 然后刷新页面
```
> 不配置也不影响其他功能，只是 AI 分析会提示"请先配置 API Key"。

---

## 7. 词汇本与复习

- **Vocab 页**：显示你存的所有生词、语境、AI 分析；
- **首页 Review 卡**：进入复习模式（SM-2 间隔重复算法，按记忆曲线安排复习时间）；
- 首页统计里的"今日待复习"就是提醒你该复习哪些词。

---

## 8. 快捷键一览

| 按键 | 功能 |
|------|------|
| `Space` | 播放 / 暂停 |
| `←` / `→` | 快退 / 快进 5 秒 |
| `B` | 显示 / 隐藏字幕挡块 |
| `L` | 锁定 / 解锁挡块 |
| `R` | 重置挡块（位置、大小、透明度、效果全部恢复默认） |
| `S` | 截图 |

（输入框里打字时快捷键不会误触发。）

---

## 9. 常见问题

| 问题 | 解决办法 |
|------|----------|
| 视频来自"文件导入"，刷新后打不开 | 刷新会让文件导入的临时地址失效。删除这条记录，重新用 New Video 添加一次 |
| 视频文件被移动/删除 | 回到首页，删除这条记录重新添加 |
| 下载视频卡住 | 保持页面打开直到完成；再试试换个网络 |
| 中文字幕没被挡住 | 调整挡块位置盖住中文行；或按 R 复位后再拖 |
| 挡块挡住了英文 | 用 L 锁定防止误碰；必要时把挡块调小一点再拖走 |
