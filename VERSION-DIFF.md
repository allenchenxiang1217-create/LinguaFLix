# 当前版本 vs GitHub 版本 差异总结

## 一句话
GitHub 远程 = `a27b1e6`（已推送的旧功能）。
本地工作区在它之上还有 **15 个文件改动（13 改 + 2 新增，+358/-42 行）**，全部是**前端渲染层**新功能与修复。

## 新增功能（4 个）

| # | 功能 | 位置 |
| --- | --- | --- |
| 1 | **字幕语言显示模式**（无字幕/双语/仅英文/仅中文，手机开关样式，自动记忆） | 播放器右侧 🌐 按钮 |
| 2 | **双语字幕自动合并**（SRT 中英文分开成块但时间一致 → 自动合并为一行双语） | 字幕解析器 |
| 3 | **视频重命名**（内联编辑，回车保存） | 视频库 + 最近学习 |
| 4 | **字幕删除**（误加载可一键清除） | 播放器顶部紧凑栏 |

## 修复（4 类）

| # | 修复 | 说明 |
| --- | --- | --- |
| 5 | 挡块不再遮挡操作栏 | 操作栏 z-20→z-50，工具栏 z-40→z-46 |
| 6 | 打开侧边栏自动隐藏字幕 | full 立即隐藏；popup 悬浮 150ms 后隐藏；关闭恢复 |
| 7 | 全屏截图反馈 | 闪光覆盖层 + 「截图已保存 · 快照 #N」toast（全屏元素内渲染） |
| 8 | 截图大图查看 | lightbox 区域扩大，等比显示 |

## 技术要点

- 视频从 `object-cover` 改回 `object-contain`：视频等比完整显示，不再裁剪（修复"打开笔记栏视频被遮挡"）
- 全屏 toast 必须渲染在**全屏元素内部**（全局 toast 被 fullscreen 层级挡住）
- 字幕合并时间容差 0.3s，英文行排前；store 持久化向后兼容

## 文档

- 更新日志：`CHANGELOG.md`
- Mac 开发者同步说明：`SYNC-NOTES-macOS.md`

## 核验清单（提交前）

- [ ] `npm run lint`（tsc --noEmit）通过
- [ ] `npm run build` 通过
- [ ] 双语字幕合并正确（英文在前）
- [ ] 字幕模式切换（无/双语/仅英文/仅中文）
- [ ] 视频重命名（视频库 + 最近学习）
- [ ] 删除字幕按钮
- [ ] 挡块不遮挡操作栏
- [ ] 侧边栏打开自动隐藏字幕
- [ ] 全屏截图有反馈 + toast
- [ ] 截图大图查看
